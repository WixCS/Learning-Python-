name = "Prince"

slice = name[0:3] #Print the values till 2 excluding the third one 
                  #Means first value is includes and last value is not
print(slice)

Length = name[2]
print(Length)